name: any-paths

dependencies:
    - lens
    - wreq

library:
    source-dirs: ./src

executable:
    main: Main.hs
    source-dirs: ./app
    dependencies:
        - any-paths

default-extensions:
    - OverloadedStrings
